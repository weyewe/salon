 
<tr id="entry-<%= object.id %>">
	<td>
		<%= "#{object.id_code}" %>
	</td>
	<td>
		<% if not object.customer_id.nil? %>
			<%= "#{object.customer.name }" %>
		<% end %>
	</td>
	
	<td>
		<%= link_to object.maintenance_histories.count, maintenance_histories_url(object) %>
	</td>
	  
	<td>
		<%= link_to root_url, :class =>"delete_entry", :style => "cursor:pointer; text-decoration:none;" do %>
			<i class='icon-remove'></i>Delete
		<% end %> 
	</td>

	<td>
		<%= link_to edit_vehicle_url(object), 
			:class =>"edit_entry modal-toggler", :style => "cursor:pointer; text-decoration:none;" do %>
			<i class='icon-edit'></i>Edit
		<% end %>
	</td>
	
</tr>