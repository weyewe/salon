<%= simple_form_for( new_object, :url => url,
      :method => :post , 
      :html => {:class => "form-horizontal prevent_double_submit ajax-form   no_content_form",
        :id => "new_object"}) do |f| %>

	<%#= f.input :parent_id , :label => "Super Category" %>
 	

 	<%= hidden_field_tag 'item_id', "#{item_id}"%>
	<%= f.input :quantity , :label => "Jumlah yang ditukar supplier", 
	 			:hint => "Jumlah yang rusak: #{item.scrap}"   %> 

  <div class="form-actions">
    <%= f.button :submit , :class => "btn btn-primary", :value=> "Create Exchange Scrap Item" %>
  </div>
<% end %>

