$(document).ready(function(){
	
	var $row = $("#entry-<%= @service_category.id %>")
	$row.spin(false); 
	
	$row.remove(); 
	
	$.msgbox("Berhasil menghapus Service Category: <%= @service_category.name %>");
	 
	
});